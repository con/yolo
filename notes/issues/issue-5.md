https://github.com/con/yolo/issues/5

# Make setup-yolo.sh do basic testing

could be to complement
- #4

as `./setup-yolo.sh` could invoke the `yolo` with some basic prompt on this repo to ensure that it all works.  Then CI actually could just run `setup-yolo.sh` in that mode
